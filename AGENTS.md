# AGENTS.md

## Version control

- Development happens in a single branch: `main`. Do not create feature branches or pull requests.
- Committing and pushing directly to `main` is allowed.

## Writing

- Name the language either `go` (in backticks) or Golang. Never write "Go" or "golang".
